# Day 34 file-agent tools — draft doc (live-update target)

This file is a deliberately stale doc. It exists so the running agent has a
real target to update live in the browser (plan step 34.10 — "read a diff +
the target .md, produce a patch, trigger the write confirmation flow").

## Danger levels

TBD — fill in from `agent-web/agent_web/services/tools/danger.py`.
