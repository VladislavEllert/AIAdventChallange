# Стартовый промпт для следующей сессии

Скопируй текст ниже целиком в новую сессию (или через `@`-упоминание этого файла):

---

Привет. Новая сессия, продолжаем неделю 7 (дни 31–35, финальная).

Прочитай в таком порядке, прежде чем что-либо делать:
1. `CLAUDE.md` — правила сессии и рабочий процесс (Обсуждение → Планирование → Реализация).
2. `memory-bank/session-context.md` — блок ⏩ ACTIVE сверху.
3. `week-07/PLAN-BRIEF.md` — ВСЕ договорённости по дням 31–35 (это главный вход).
4. `AGENTS.md` — харнес `/plan /build /review /debug`.
5. Конспект лекции недели 7 в `memory-bank/lessons/` (если ещё не проиндексирован — учти при плане).

Фаза обсуждения по неделе 7 УЖЕ завершена — всё зафиксировано в `PLAN-BRIEF.md`. Ничего не
переобсуждаем заново, если я сам не подниму.

Задача этой сессии: написать план на ВСЕ 5 дней сразу через `/plan` (Opus 4.8, medium effort,
planner + skeptic). Опирайся строго на `week-07/PLAN-BRIEF.md`. Требования к плану оттуда же:
пошагово, всё покрыть тестами (pytest), агент сам тестирует через браузер (Playwright — при
необходимости написать ему скилл), «готово» только после реального теста, полный доступ к
терминалу, минимум моего участия. Кодить потом будем Sonnet 5 medium по фазам, затем `/review`.

Модель уже должна быть Opus 4.8 (`/model claude-opus-4-8`). Начинай с `/plan` по брифу — но
сам код НЕ пиши, пока я не дам явную отмашку.

---

## Памятка (для меня, юзера)

- Перед запуском: `/model claude-opus-4-8`, эффорт medium.
- Команда: `/plan` + текст выше (или просто «пиши план по week-07/PLAN-BRIEF.md на все 5 дней»).
- Если Opus начнёт кодить без плана — стоп, это нарушение workflow (см. память
  `feedback-no-code-before-plan`).
