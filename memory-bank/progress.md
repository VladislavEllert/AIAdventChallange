# Прогресс

Статусы: `todo` / `done`.

| Неделя | День | Задача | Статус | Код | Видео |
|--------|------|--------|--------|-----|-------|
| 01 | 01 | Первый запрос к LLM через API (Telegram-бот на Gemini) | done | [week-01/day-01](../week-01/day-01/) | [▶](https://www.loom.com/share/45330da8e4494e6ca49480a3420ff787) |
| 01 | 02 | Формат ответа: сравнение без/с ограничениями (формат, длина, stop) | done | [week-01/day-02](../week-01/day-02/) | [▶](https://www.loom.com/share/866d3823571646c698e02f7b201d084b) |
| 01 | 03 | Разные способы рассуждения: 4 метода (прямой / пошагово / meta / эксперты) на задаче-ловушке «автомойка» | done | [week-01/day-03](../week-01/day-03/) | [▶](https://www.loom.com/share/20f5f9c908834b5581588171f83c1cc2) |
| 01 | 04 | Температура: сравнение 0/0.7/1.2 (+1.5/2.0) по точности/креативности/разнообразию; конвейер выбора слова + эксперимент top_p/top_k | done | [week-01/day-04](../week-01/day-04/) | [▶](https://www.loom.com/share/70d345242d1446a8ada8343cbc50eb83) |
| 01 | 05 | Версии моделей: 1 промпт-ловушка на 4 GPT (3.5/4o/4.1/o3), замер latency/токенов/₽; эксперимент «точность промпта × сила модели» | done | [week-01/day-05](../week-01/day-05/) | [▶](https://www.loom.com/share/80198dddfa8644c5b92ebf0823304d0c) |
| 02 | 06 | Первый агент: iOS-приложение AgentChat (SwiftUI), мульти-агент (Аладдин/Акс/Шут) поверх ProxyAPI, память, фото, выбор моделей | done | [week-02/day-06](../week-02/day-06/) · [AgentChat](../AgentChat/) | [▶](https://drive.google.com/file/d/1oxYBNidlMzq5eve09l61oSuv6t2t1pWy/view?usp=sharing) |
| 02 | 07 | Сохранение контекста: SQLite (SwiftData) + слоистая память — долгие факты между сессиями, сжатие в summary, JSON-экспорт | done | [week-02/day-07](../week-02/day-07/) · [AgentChat](../AgentChat/) | [▶](https://www.loom.com/share/f35016674e984ab6a81f10998e507ba0) |
| 02 | 08 | Токены и контекстное окно: подсчёт токенов (вход/выход/итого + ₽), рост стоимости, стриминг (SSE), компактация в summary, демонстрация поломки при переполнении | done | [week-02/day-08](../week-02/day-08/) · [AgentChat](../AgentChat/) | [▶](https://www.loom.com/share/2f4052d188a34c45a730ead89fb72a7e) |
| 02 | 09 | Сжатие истории: окно последних N + старое в summary (отдельно, подставляется вместо полной истории), тумблер сжатия для сравнения качества/токенов без и со сжатием | done | [week-02/day-09](../week-02/day-09/) · [AgentChat](../AgentChat/) | [▶](https://www.loom.com/share/cbeff47284d641ad913540f47d3b14e1) |
| 02 | 10 | Управление контекстом: 3 стратегии без summary (Sliding Window / Sticky Facts KV / Branching) как 3 тест-агента; раздел «Мои \| Тестовые»; ветки с форком/переключением/удалением | done | [week-02/day-10](../week-02/day-10/) · [AgentChat](../AgentChat/) | [▶](https://www.loom.com/share/2383219a98a4483cb535fb2230070678) |
| 03 | 11 | Модель памяти: 3 слоя (краткосрочная / рабочая / долговременная), авто-извлечение, MemorySheet UI | done | [week-03/day-11](../week-03/day-11/) · [AgentChat](../AgentChat/) | [▶](https://www.loom.com/share/448df13307fd48d991a8dd3fd7d1da4a) |
| 03 | 12 | Персонализация: слоистый профиль юзера (стиль/правила/стек), подключение к каждому запросу, 2 профиля → разные ответы | done | [agent-cli](../agent-cli/) | [▶](https://www.loom.com/share/46f41dfce33b4181bed14f6802d003f3) |
| 03 | 13 | Task State Machine: 4 стадии-агента (planning→execution→validation→done), детерминированные переходы, пауза/resume | done | [agent-cli](../agent-cli/) | [▶](https://www.loom.com/share/e516f7eb3f634c52b6870968d3103ac2) |
| 03 | 14 | Инварианты: правила отдельно от диалога, инжект в промпт, проверка код+LLM, отказ нарушать | done | [agent-cli](../agent-cli/) | [▶](https://www.loom.com/share/216ee8cfa37f404087023ccd88672676) |
| 04 | 15 | Контролируемые переходы: рой 3 агентов (PLANNING) + Оркестратор-надсмотрщик на всех стадиях, именованные сессии SQLite с персистом, `/session` команды, `/task jump` FSM-демо | done | [agent-cli](../agent-cli/) | todo |
| **Web** | **15+** | **Веб-приложение:** FastAPI + React, портирует все CLI функции, SSE стриминг, Task FSM UI, Invariants check+rollback, Profile инжекция, Model switching | **done** | [agent-web](../agent-web/) | **todo** |
| 04 | 16 | MCP: FastMCP-сервер на VPS (194.226.115.120:8001), 3 инструмента, day16_connect.py → список tools | done | [mcp-server](../mcp-server/) | todo |
| 04 | 17 | MCP: agent-web интегрирует tools (tool_start/tool_done SSE), web_search + get_moex_quote → живые ответы | done | [agent-web](../agent-web/) · [mcp-server](../mcp-server/) | todo |
| 04 | 18 | MCP: APScheduler кэш MOEX каждые 30 сек, get_moex_summary, тесты server + mcp_client | done | [agent-web](../agent-web/) · [mcp-server](../mcp-server/) | todo |
| 05 | 21 | RAG: 24 стр. GitLab Handbook (93k слов) → fixed+structural chunking → Ollama nomic-embed-text (768-dim) → 2 JSON индекса | done | [agent-web/scripts/rag](../agent-web/scripts/rag/) | [▶](https://drive.google.com/drive/folders/1Buhxb6gJ6stIYDTxpYsWL0Ce9rAinH3H?usp=share_link) |
| 05 | 22 | RAG запрос: тумблер RAG в UI (◈), ретрив top-5 → working_context → стрим; 10 вопросов eval; results_day22.md | done | [agent-web](../agent-web/) | [▶](https://drive.google.com/drive/folders/1Buhxb6gJ6stIYDTxpYsWL0Ce9rAinH3H?usp=share_link) |
| 05 | 23 | Query rewrite (перевод на английский) + cosine threshold filter; rag_meta SSE; filter badge в UI; сравнение RAG без фильтра / с фильтром, results_day23.md | done | [agent-web](../agent-web/) | [▶](https://drive.google.com/drive/folders/1Buhxb6gJ6stIYDTxpYsWL0Ce9rAinH3H?usp=share_link) |
| 05 | 24 | Sources SSE → SourcesBlock (URL + score + цитаты); don't-know gate (best_score<0.55 → отказ без вызова LLM); citation instruction в system prompt. Отдельного results_day24.md нет — вопросы те же, что в дне 23 (results_day23.md), там уже видны sources/цитаты на каждом ответе | done | [agent-web](../agent-web/) | [▶](https://drive.google.com/drive/folders/1Buhxb6gJ6stIYDTxpYsWL0Ce9rAinH3H?usp=share_link) |
| 05 | 25 | Task memory поверх RAG-чата: `task_state.py` — LLM отдаёт только дельту (новый факт/constraint), накопление в коде; goal фиксируется один раз, не дрейфует; clarified_facts = факты о юзере + темы вопросов; constraints ловит инструкции типа "отвечай по-русски". Проверено на 2 сценариях по 10 ходов, results_day25.md | done | [agent-web](../agent-web/) | [▶](https://drive.google.com/drive/folders/1Buhxb6gJ6stIYDTxpYsWL0Ce9rAinH3H?usp=share_link) |
| Infra | 06 | Windows-сессия подняла локальный AI-сервер (RTX 4060, 8GB): Ollama `qwen3:4b` (LAN 192.168.0.33:11434), ComfyUI+SDXL base 1.0 (LAN :8188, тест-генерация 1024×1024 прошла на GPU с `--lowvram`), firewall-правила на 3 порта. Метрик-агент (:11435) — пишет Mac-сессия, ещё нет. См. [week-06/WINDOWS_SETUP.md](../week-06/WINDOWS_SETUP.md) | done (Windows-часть) | [week-06](../week-06/) | todo |
| 06 | 26 | Local LLM smoke: 3 живых запроса (факт/рассуждение/код) в `qwen3:4b` через LAN, `agent-cli/tests/test_ollama_smoke.py` (skip если сервер недоступен). Гочта: root `.env` затенял `agent-web/.env` для `OLLAMA_CHAT_URL` — фикс явным путём | done | [week-06/day-26](../week-06/day-26/) | todo |
| 06 | 27 | DispatchProvider (ollama/*→Ollama, comfyui/*→ComfyUI, иначе ProxyAPI), общий OpenAICompatProvider базовый класс, ComfyUI image-клиент (poll /history вместо WS — проще для sync SSE generator), text↔image в одном чате. Живая проверка: Ollama-чат + ProxyAPI-чат оба реально стримят, tool-calling на Qwen3 подтверждён, картинка реально сгенерирована. Найдены и исправлены 2 бага: dotenv frame-based search резолвил config.py в agent-cli/.env вместо agent-web/.env (Ollama тихо шёл на localhost); `isinstance(provider, ProxyAPIProvider)` в respond_stream_with_stats ломал streaming под DispatchProvider. VRAM-контеншн подтверждён: Qwen3+SDXL параллельно ~215с vs ~44с соло | done | [week-06/day-27](../week-06/day-27/) | todo |
| 06 | 28 | RAG generation уже шла через agent.model (готово с дня 27) — закрыт последний облачный хвост: query-rewrite скипается для ollama/* (Qwen3 thinking-модель, `<think>` съедает max_tokens до 1500 и не отвечает, `think:false`/`/no_think` не работают на этой сборке Ollama). Один прогон сравнения с/без RAG на qwen3:4b (results_day28.md) — честная находка: без rewrite кросс-язычный embedding-поиск промахивается (cloud с rewrite → time-off, локаль без rewrite → случайные разделы), don't-know gate всё равно спасает от галлюцинации | done | [week-06/day-28](../week-06/day-28/) | todo |
| 06 | 29 | Панель настроек (text: temperature/max_tokens/top_p/num_ctx; image: steps/cfg/seed/размер), `metrics_server.py` на Windows (psutil+nvidia-smi+ollama/api/ps) + `/api/metrics` прокси-кэш + HUD в статус-баре. Кванты отложены (юзер решил не докачивать сейчас, SSD почти забит). Баги: тест без изоляции затирал реальный settings.json (пофикшено tmp_path фикстурой); метрика-прокси висела 3с/запрос (remote unreachable ≠ instant refused, таймаут 3.0→0.8с). Мобильный адаптив сделан в этот же день (сквозная задача) — реальными скриншотами Playwright найдена полностью сломанная мобилка, исправлено drawer-панелями + 44px тапами + iOS zoom fix | done | [week-06/day-29](../week-06/day-29/) | todo |
| 06 | 30 | Приватный сервис: `__main__.py` host/port/reload из env (default 0.0.0.0:8765, reload off), прод-сборка same-origin, rate-limit (30/60с на IP, свой in-memory, без slowapi — не internet-facing). Живьём: прод-сборка поднята и доступна по LAN, реальный чат через Ollama, 3 параллельных запроса к живому Ollama не падают, ответы не путаются между сессиями, но честно замерена сериализация на одной GPU (~2с генерация, ~17-21с wall при параллели — очередь не параллелизм). Windows-деплой НЕ выполнен этой сессией (нет терминального доступа) — раннбук в WINDOWS_SETUP.md §ШАГ 11, юзер сделает git clone + запуск сам. Баг в собственном тесте rate-limit (боролся с реальными часами) найден и исправлен | done | [week-06/day-30](../week-06/day-30/) | todo |
| Infra | 06 | Windows-деплой (§ШАГ 11) доведён до конца второй Windows-сессией. Живьём проверено на реальном железе: чат `ollama/qwen3:4b` (стрим, `cost_rub=0`, `<think>` не течёт в ответ), картинка `comfyui/sdxl` (реальный PNG), RAG retrieval (sources/score верные), MCP (VPS Finance 9 тулов), `/api/metrics` (реальный HUD), мобилка (drawer-панели), LAN-доступ. Найдены и исправлены 3 Windows-специфичных бага (не в коде Mac — среда): системный SOCKS-прокси ломал `httpx`/OpenAI SDK на каждом локальном вызове (Ollama/ComfyUI/metrics/MCP) → `trust_env=False` везде, где создаётся клиент для local/LAN/VPS; `DispatchProvider` жадно требовал `PROXYAPI_KEY` даже без облачных моделей → ленивая конструкция; `sessions.db` повреждена резким `taskkill /F` поверх открытой WAL-транзакции → пересоздана (важных данных не было). Модели перенесены на `D:\OllamaModels\models` (HDD, SSD был почти забит), докачан `qwen3:8b` (7.5GB) для сравнения. Публичный доступ (Cloudflare Tunnel) заблокирован на сетевом уровне (QUIC+HTTP/2 до argotunnel.com не проходят) — отложено по решению юзера, LAN работает. Подробности: [week-06/WINDOWS_SETUP.md](../week-06/WINDOWS_SETUP.md) §ШАГ 11 | done | [week-06](../week-06/) | todo |
| 07 | 31 | Вторая RAG-база (`KNOWLEDGE_BASES` реестр handbook+project в `rag/config.py`, `get_rag_index(kb=...)` кеш по базам, `load_index` падает громко на рассинхроне `dim`) + `embedder.embed(text, backend=...)` (ollama 768d / proxyapi 512d, хардкод убран) + корпус проекта (`build_project_corpus.py`: README/CLAUDE.md/AGENTS.md/memory-bank/**/исходники + дамп `app.openapi()` → `api_openapi.md`, 147 файлов) + `build_index.py --project` строит оба индекса одной командой (ollama пропущен нелетально — LAN-бокс недели 6 не поднят на этой машине, `index_project_proxy.json` собран живым вызовом ProxyAPI, 194 чанка/2.9МБ) + локальный MCP-сервер проекта (`mcp-server/project_server.py`, 127.0.0.1:8002, `git_current_branch`/`git_status`/`git_diff`/`list_project_files`, сандбокс на `PROJECT_ROOT` через `is_relative_to`) + команда `/help` (реестр `services/commands.py` только для новых команд, 4 старые не тронуты) с RAG по проекту + реальной git-веткой через MCP, `threshold_answer=0.33` откалиброван на 10 вопросах (in-KB 0.35-0.63 vs out-of-KB 0.22-0.31). Живой баг найден браузерным прогоном (Playwright), не догадкой: `ChatInput.tsx` уже перехватывал `/help` клиентским статичным списком команд безусловно, `/help <вопрос>` никогда не доходил до бэкенда — pytest этого не поймал бы (бьёт прямо по `/api/chat/stream`, минуя фронтенд). Исправлено: no-args `/help` остаётся клиентским, `/help <вопрос>` падает в обычную отправку | done | [week-07/day-31](../week-07/day-31/) | todo |
| 07 | 32 | AI-ревью PR headless: `review/pipeline.py` (`run_review()`, diff-парсер без LLM с номерами строк, RAG-запросы по файлу/символу, `chat_fn`-инъекция для тестируемости), `review/resilience.py` (лесенка timeout→retry primary→fallback-модель→`"review failed, needs human"`, никогда не бросает исключение), `review/metrics.py` (`data/review_metrics.jsonl`, `aggregate()` p50/p95/p99/cost/%успешных/quality gate по последним N *оценённым*), `review/__main__.py` (CLI: `--diff-file`/`--base`+`--head`, `--model`, `--dry-run`, `--post-comment`, подкоманда `score <run_id> <1-5>`). `.github/workflows/ai-review.yml`: `on:[pull_request, workflow_dispatch]`, guard на форки, ключ только `${{ secrets.PROXYAPI_KEY }}`, деградация в понятный комментарий вместо красного CI при отсутствии секрета. 4 эталонные фикстуры (`review_eval/fixtures/`) из реальных багов недели 6 (dotenv frame-based резолюция не того `.env`; `isinstance(provider, ProxyAPIProvider)` ломается под `DispatchProvider`-обёрткой; `httpx.Client` без `trust_env=False` ловит SOCKS) + 1 чистая (false-positive rate). Живой eval (`review_eval/run_eval.py`, реальные вызовы ProxyAPI, `openai/gpt-4o-mini`): **recall 67% (2/3 — модель реально пропустила httpx/trust_env баг, не keyword-артефакт)**, FP-rate 0% (0/1), latency p50 2845ms/p95 3476ms/p99 3559ms, cost/прогон 0.0457₽, % успешных 100%, RAG-индекс content-hash `53b73380a53c` записан в `results_day32.md` для сверки после пересборки корпуса днём 33. `pytest agent-web/tests`: 118 passed (0 живых вызовов вне eval-харнесса). Свой баг найден и исправлен до коммита: `path: Path = METRICS_PATH` как default-параметр биндился при импорте модуля — monkeypatch `METRICS_PATH` в тестах молча игнорировался, тестовые записи утекали в реальный `data/review_metrics.jsonl`; исправлено на резолюцию внутри тела функции, утечка вычищена вручную. Сокращено против брифа: 3 живых синтетических PR (шаг 35.5) и вторая версия промпта (35.6) перенесены в день 35 — живой триггер `pull_request`→комментарий бота НЕ проверен (нужен реальный PR + GitHub Secret `PROXYAPI_KEY` от юзера) | done | [week-07/day-32](../week-07/day-32/) | todo |
| 07 | 33 | Ассистент поддержки: `data/support/tickets.json` — 9 тикетов из реальных багов недели 6-7 (SOCKS-прокси на Windows ломает `httpx`/OpenAI SDK локально, `sessions.db` повреждена `taskkill /F` поверх WAL, qwen3 пустой ответ из-за `<think>` съедающего `max_tokens`, мобильная вёрстка, `/api/metrics` висел 3с, dotenv frame-based резолюция не того `.env`, `isinstance(provider, ProxyAPIProvider)` ломает стриминг под `DispatchProvider`, эагерный `PROXYAPI_KEY`, тест затирал боевой `settings.json`), поля id/title/product_area/version/environment/symptom/steps/status/user/history. `data/support/faq.md` (симптом→причина→фикс на каждый класс) — `build_project_corpus.py` дополнен `_dump_faq()` (тот же приём что `_dump_openapi()` дня 31), копирует в `corpus_project/faq.md` под стабильным именем при каждой пересборке; корпус пересобран, 155 файлов (было 147). `mcp-server/project_server.py`: `list_tickets(status)`, `get_ticket(id)` — `search_tickets` сознательно не реализован (план исключает). `services/commands_support.py`: `/support <ticket_id> <вопрос>` — тикет через реальный MCP-вызов `get_ticket` (тот же паттерн проверки `_tool_registry`, что `/help`'s git-ветка) → `[КОНТЕКСТ ТИКЕТА]` (environment/version/symptom/status/steps/history) в system-промпте → RAG(`kb=project`). `pytest agent-web/tests`: **135 passed** (было 118), включая тест что `environment`/`version`/`symptom` тикета реально попадают в system-сообщение LLM (не просто получены и отброшены). Блокер предыдущей сессии (ProxyAPI баланс = 0, `402 Insufficient balance`) — закрыт после пополнения баланса юзером 2026-07-18. Доведено вживую: `build_index.py --project` пересобрал `index_project_proxy.json` (155 файлов, 207 чанков, content-hash `53b73380a53c` → `4f7971c234d9`; ollama/768d-индекс по-прежнему пропущен — LAN-бокс 192.168.0.33:11434 недоступен, graceful fallback на proxyapi/512d сработал как задумано). `run_eval.py` перезапущен, числа **дописаны** (не перетёрты) в `results_day32.md` вторым прогоном — recall 67%→33% (фикстура 02 перестала флагаться, тот же промпт/модель, другой индекс + LLM run-to-run разброс, зафиксировано как заметка, не расследовано). Эталонный сценарий 33.5: реальный `/support TICKET-001 как починить прокси на Windows?` вернул environment-специфичный ответ ("Windows 11, RTX 4060 8GB, Python 3.x venv и системный SOCKS-прокси... `trust_env=False`") — дословно поля тикета, не общий совет. Добавлен `agent-web/frontend/e2e/support-command.spec.ts`, полный Playwright-прогон 4/4 passed (golden-path, help-command×2, support-command), скриншот в `week-07/day-33/screens/support-ticket-001.png`. `/help` и день-32 review CLI тоже проверены живьём — не сломаны остаточным состоянием после 402. Найден (не исправлен, вне скоупа): `rag_search()` в `/help` и `/support` не оборачивает вызов эмбеддера в try/except. `pytest agent-web/tests`: 135 passed | done | [week-07/day-33](../week-07/day-33/) | todo |
| 07 | 34 | Файловый агент + human-in-the-loop. **34.1 control-flow фикс в `chat.py` (сделан первым, самый рискованный шаг недели):** RAG-ветка раньше безусловно `return`-ила сразу после стриминга — цикл тул-коллинга ниже был физически недостижим при `use_rag=True`. Исправлено хирургически: fast-path (`use_rag and not use_mcp`) сохранён байт-в-байт (та же логика, тот же `return`), а при `use_mcp=True` RAG-ветка вместо `return` собирает `rag_context_block`/`rag_label`/`rag_task_state_block` и падает через в тул-луп, где контекст инжектится в system-сообщение перед первым LLM-вызовом с тулами. Regression-тест (`test_chat_rag_plus_tools.py`) доказывает: `use_rag=True,use_mcp=True` теперь доходит до 2-го (тул-луп) LLM-вызова — до фикса физически не мог. Полный regression-прогон дней 31-33 (135 тестов) — зелёный, не задет. Новый пакет `agent-web/agent_web/services/tools/`: `registry.py` (`Tool` dataclass, схема идентична MCP-формату), `danger.py` (чистая функция без LLM, `dict[str,str]` lookup, неизвестный тул → `dangerous`, fail-closed), `fs_tools.py` (`read_file`/`search_files`(ripgrep)/`list_dir` safe, `write_file`(dry_run=True по умолчанию, поддержан реальный режим)/`delete_file` dangerous; песочница `Path.resolve()`+`is_relative_to(REPO_ROOT)` — НЕ строковый префикс, отдельный деньлист `.env`/`*.key`/`.git/**`/`.venv/**`/`node_modules/**` даже для путей формально внутри репо), `confirm.py` (poll-loop `while elapsed<timeout: event.wait(poll_interval)` вместо блокирующего `wait(120)`, keepalive-SSE инертен для парсера `chat.ts`, auto-deny по таймауту И на протухшей "stream session" — отдельной от `session_id` сущности на один SSE-запрос), `executor.py` (`execute_stream()`/`execute()`, исключения из тула никогда не пробрасываются наружу). `POST /api/tools/confirm` + `GET /api/tools` в новом `routers/tools.py`, зарегистрирован в `app.py`. Фронтенд: `chat.ts` новые SSE-события `confirm_request`/`confirm_result`, `ConfirmToolModal.tsx` (имя тула/аргументы/причина, Разрешить/Отклонить, Esc=Отклонить, без diff-превью — план прямо называет это gold-plating), `useChatStore` — `pendingConfirm`. **Живая проверка (мандаторная часть фазы, самая сложная):** сценарий А (search_files→read_file×N→unified diff, найдены реальные разные стили сообщений об ошибках тулов в `chat.py`, ничего не записано) — пройден в реальном браузере. Сценарий Б негативный (Отклонить → `git status` чист) и **позитивный** (Разрешить → файл реально изменён на диске, `git status` A→AM staged+modified, восстановлен после проверки) — оба сняты и в реальном Chromium-браузере (скриншоты), и напрямую через `/api/chat/stream` curl. Целевой файл для Б — `week-07/day-34/TOOLS_DOC_DRAFT.md`, специально `git add`-нут (не закоммичен) заранее, чтобы иметь небезразличный git-статус-бейзлайн (`??` untracked иначе не различил бы "до" и "после"). Честно задокументирована LLM-вариативность tool-calling решения: не каждый прогон gpt-4o-mini реально решает вызвать `write_file` (иногда просто отвечает уточняющим вопросом вместо вызова) — confirm/executor-код при этом детерминирован и покрыт unit-тестами (`test_confirm.py`, `test_tools_executor.py`, реальные потоки, без ASGI) независимо от поведения живой модели. `pytest agent-web/tests`: **178 passed** (было 135, +43: sandbox/danger/confirm/executor/tools_router/rag_plus_tools), 0 живых вызовов вне explicitly-marked live. Playwright: 7/7 passed (golden-path, help×2, support, tool-confirm×3). Не тронуто (вне скоупа): `rag_search()` try/except gap дня 33 — живёт в `commands_help.py`/`commands_support.py`, не в общем `chat.py` RAG-пути, который день 34 переписывал (тот путь уже был обёрнут в try/except и до, и после правки). Требует ОДИН uvicorn worker — confirm-стор модульный in-memory dict | done | [week-07/day-34](../week-07/day-34/) | todo |
| 07 | 35 |  | todo | [week-07/day-35](week-07/day-35/) | todo |

> **Пивот (день 12+):** переходим с iOS (`AgentChat/`) на CLI/TUI на Python (`agent-cli/`).
> AgentChat остаётся, не удаляем. Причина: недели 4–7 (MCP/RAG/VPS/пайплайны) iOS не тянет.
>
> **День 15+ (Web):** Параллельно CLI разработан web UI, портирует 100% функционала CLI.
> Архитектура готова к неделям 4–7 (MCP инструменты, RAG, VPS демо).
